import React from "react";
import { TypingEffect } from "../../lib";

const Messages = ({ message, setMessage }) => {
  return (
    <div className="w-full flex flex-col relative overflow-x-auto mt-32">
      {/* {message && <TypingEffect />} */}
    </div>
  );
};

export default Messages;
